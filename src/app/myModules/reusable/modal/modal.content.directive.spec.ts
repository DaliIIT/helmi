import { Modal.ContentDirective } from './modal.content.directive';

describe('Modal.ContentDirective', () => {
  it('should create an instance', () => {
    const directive = new Modal.ContentDirective();
    expect(directive).toBeTruthy();
  });
});
