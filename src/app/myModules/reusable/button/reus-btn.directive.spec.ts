import { ReusBtnDirective } from './reus-btn.directive';

describe('ReusBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ReusBtnDirective();
    expect(directive).toBeTruthy();
  });
});
