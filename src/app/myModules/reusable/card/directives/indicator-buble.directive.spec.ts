import { IndicatorBubleDirective } from './indicator-buble.directive';

describe('IndicatorBubleDirective', () => {
  it('should create an instance', () => {
    const directive = new IndicatorBubleDirective();
    expect(directive).toBeTruthy();
  });
});
